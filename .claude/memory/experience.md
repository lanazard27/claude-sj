# 경험 기반 학습 (Experiential Learning)

> 작업 방식 선택 단계에서 참고하는 실패/성공 이력.
> 새 세션 시작 시 이 파일을 체크하여 go-to 방식을 우선 적용한다.
> 기록 규칙: 실패 3회 누적 → 인사이트로 일반화 → CLAUDE.md 규칙화 제안

---

## 작성 포맷

```
## 카테고리: [작업 유형]
- ❌ [방식/도구]: [실패 이유] (X회 실패) → reflexion: "[왜 실패했는지]"
- ✅ [방식/도구] → go-to
- 인사이트: "[일반화된 교훈]"
```

---

## 카테고리: PDF 생성 (2026-04-15)
- ✅ puppeteer-core + Chrome + HTML → go-to
- 비고: macOS에 Chrome 설치된 경우 puppeteer-core만으로 충분 (Chromium 다운로드 불필요)
- 비고: Google Fonts (@import) 사용 시 `waitUntil: 'networkidle0'` + `document.fonts.ready` 대기 필수
- 비고: `printBackground: true` 설정해야 CSS 배경색/그라데이션 정상 출력
- ❌ subdirectory에서 Write 시 hook 경로 문제: `.claude/hooks/` 상대 경로가 CWD 기준 → 항상 프로젝트 루트에서 Write 실행 필요
- 인사이트: "PDF 생성은 HTML+CSS로 디자인 후 puppeteer-core로 변환이 가장 유연. Chrome 설치된 환경이면 puppeteer-core만으로 충분"

## 카테고리: 병렬 구현 품질 (2026-04-20)
- ❌ code-writer 1명에게 11개 파일(컴포넌트 전부) 위임: 후반부 퀄리티 급락, 임시 구현 다수
- ❌ 레이어별 수평 분할(엔진/렌더러/UI): Phase 2 연결 작업 많고, 각 에이전트가 "전체 기능의 일부분"만 담당해 맥락 부족
- ✅ 스펙 단위 수직 분할(기능별 3-6파일): 각 에이전트가 완결된 기능 전체 담당 → 맥락 이해도 높음, Phase 2 최소화
- ✅ Phase 0 공통 기반(types/constants/hooks) 선행 생성: 파이프라인 간 계약서 역할
- 인사이트: "에이전트당 파일 수가 품질을 결정한다. 3-6개가 적정. 스펙 단위 분할이 레이어 분할보다 낫다"

## 카테고리: 워크플로우 디테일 (2026-04-20)
- ❌ "RPG 만들어" → 바로 구현: 요구사항 누락, 엣지케이스 무시, 에러 처리 빈약
- ✅ PRD → 아키텍처 → 참고코드 → 테스트명세 → 구현: 각 단계에서 디테일이 누적됨
- ✅ 참고 구현체(오픈소스 코드)를 프롬프트에 직접 포함: "이렇게 만들어" 보다 "이 코드처럼 만들어"가 10배 정확
- 인사이트: "PRD+아키텍처+참고코드+테스트명세를 전부 프롬프트에 넣으면 code-writer가 거의 질문 없이 정확히 구현함"

## 카테고리: 리뷰 효율 (2026-04-20)
- ✅ 10파일+ 시 per-pipeline 리뷰(병렬) → 통합 리뷰(1명): 깊이 + 전체 맥락 모두 확보
- 인사이트: "리뷰도 구현처럼 분할하면 깊이 있는 검증 가능. 단, 통합 리뷰는 반드시 1명이 전체 맥락으로"

<!-- 아래는 삭제하지 마세요. 새 경험 기록 시 이 위에 추가하세요. -->
