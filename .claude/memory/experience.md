# 경험 기반 학습 (Experiential Learning)

> 작업 방식 선택 단계에서 참고하는 실패/성공 이력.
> 새 세션 시작 시 이 파일을 체크하여 go-to 방식을 우선 적용한다.
> 기록 규칙: 실패 3회 누적 → 인사이트로 일반화 → CLAUDE.md 규칙화 제안

---

## 작성 포맷

```
## 카테고리: [작업 유형]
- ❌ [방식/도구]: [실패 이유] (X회 실패) → reflexion: "[왜 실패했는지]"
- ✅ [방식/도구] → go-to
- 인사이트: "[일반화된 교훈]"
```

---

## 카테고리: 구현/리뷰 품질 (2026-04-20)
- ❌ code-writer 1명에게 11개 파일 위임: 후반부 퀄리티 급락, 임시 구현 다수
- ❌ 레이어별 수평 분할(엔진/렌더러/UI): Phase 2 연결 작업 많고 맥락 부족
- ✅ 스펙 단위 수직 분할(기능별 3-6파일): 각 에이전트가 완결된 기능 전체 담당 → 맥락 이해도 높음
- ✅ Phase 0 공통 기반(types/constants/hooks) 선행 생성: 파이프라인 간 계약서 역할
- ✅ 10파일+ 시 per-pipeline 리뷰(병렬) → 통합 리뷰(1명): 깊이 + 전체 맥락 모두 확보
- 인사이트: "에이전트당 3-6파일이 품질 적정. 스펙 단위 수직 분할이 레이어 분할보다 낫다. 리뷰도 구현처럼 분할하되 통합은 1명이"

## 카테고리: 워크플로우 디테일 (2026-04-20)
- ❌ "RPG 만들어" → 바로 구현: 요구사항 누락, 엣지케이스 무시
- ✅ PRD → 아키텍처 → 참고코드 → 테스트명세 → 구현: 각 단계에서 디테일 누적
- ✅ 참고 구현체를 프롬프트에 직접 포함: "이렇게 만들어"보다 "이 코드처럼 만들어"가 10배 정확
- 인사이트: "PRD+아키텍처+참고코드+테스트명세를 전부 프롬프트에 넣으면 code-writer가 거의 질문 없이 정확히 구현함"

## 카테고리: 대규모 프로젝트 관리 (2026-04-22)
- ❌ 컨테이너마다 PRD 순차 작성: 매번 코드 재독 + 컨텍스트 재적재로 이해 시간 반복
- ✅ 전체 PRD 선작성 (C0~CN): Phase 1에서 스키마+타입+PRD 전부 작성, Phase 2에서 순차 구현만 — 이해 시간 1회로 단축
- ❌ haiku 모델 파일 생성 신뢰도 낮음: "DONE" 보고하면서 실제 파일 생성 안 함 — sonnet 이상 사용 권장
- ❌ 병렬 writer 간 CSS 클래스명 불일치: Writer 2는 `playlist-item`, Writer 3은 `playlist-save-item` → 통합 시 UI 깨짐
- 인사이트: "병렬 writer에게 공통 CSS 클래스명 목록을 Phase 0에서 미리 정의. haiku는 파일 생성 신뢰도 낮아 sonnet 이상 필수"

## 카테고리: PDF 생성 (2026-04-15)
- ✅ puppeteer-core + Chrome + HTML → go-to (macOS Chrome 환경에서 Chromium 다운로드 불필요)
- 인사이트: "PDF는 HTML+CSS 디자인 후 puppeteer-core 변환이 최적. `printBackground: true`, Google Fonts 시 `networkidle0`+`document.fonts.ready` 대기 필수"

<!-- 아래는 삭제하지 마세요. 새 경험 기록 시 이 위에 추가하세요. -->
